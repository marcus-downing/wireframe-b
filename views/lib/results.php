<?php


/**
 * Write search results.
 *
 * <code>
 * \wireframe_b\the_results();
 * </code>
 *
 * Use the `results/*.php` view parts to display a list of results from a search or query.
 *
 * @param array $results  Optional list of results. If absent, will show results from the current WordPress loop instead.
 */
function the_results($results = null) {
  if (is_null($results)) {
    
  }
}